// prettier.config.js

// prettier.config.js

export default {
  plugins: ["prettier-plugin-tailwindcss"],
};

// Try doing an export default to the plugin in your prettier.config file and change the file extension to mjs instead of js or cjs as mentioned from jonas
